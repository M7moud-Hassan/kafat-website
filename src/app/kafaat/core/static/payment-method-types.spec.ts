import { PaymentMethodTypes } from './payment-method-types';

describe('PaymentMethodTypes', () => {
  it('should create an instance', () => {
    expect(new PaymentMethodTypes()).toBeTruthy();
  });
});
