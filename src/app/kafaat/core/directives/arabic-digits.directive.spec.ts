import { ArabicDigitsDirective } from './arabic-digits.directive';

describe('ArabicDigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new ArabicDigitsDirective();
    expect(directive).toBeTruthy();
  });
});
